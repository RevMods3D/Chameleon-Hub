<Window x:Class="Chameleon_Hub.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" Height="197" Width="350" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" >
    <Grid>
        <Border CornerRadius="20"></Border>
        <Image Source="/Resources/LoadingScreen.png" Stretch="Fill" />

        <Label x:Name="labelStatus" Content="Initializing..."
               Foreground="#FF525252" Background="Transparent"
               FontSize="10" HorizontalAlignment="Center"
               VerticalAlignment="Bottom" Margin="0,0,0,5"/>

        <ProgressBar x:Name="progressBar" Height="5"
                     Width="350" Minimum="0" Maximum="100"
                     IsIndeterminate="False"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Bottom" Background="#FF0E0E0E" BorderBrush="#00BCBCBC" Foreground="#FF404040"/>
    </Grid>
</Window>
